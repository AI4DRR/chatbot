# Development tooling only — never installed in the runtime image.
# Runtime dependencies remain in requirements.txt (inherited baseline).
-r requirements.txt
ruff>=0.6,<1
mypy>=1.10,<2
pytest>=8,<9
httpx>=0.27,<1
